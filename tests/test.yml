---
- hosts: all
  become: yes
  roles: 
    - role: role_under_test
  pre_tasks:
  - name: install role dep joe_speedboat.ansible_mariadb
    shell: ansible-galaxy install -p ./roles joe_speedboat.ansible_mariadb
    changed_when: False

...
